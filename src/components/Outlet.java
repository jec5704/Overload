package components;
/**
 * this class just creates an outlet constructor
 *
 * @author Julio Cuello
 */
/**
 * this metho is the constructor of the outlet and it uses the super or
 * the constructor of the component class and it creates an Outlet instance
 */
public class Outlet extends Component {
    public Outlet(String name, Component source) {
        super(name, source);
    }
}
